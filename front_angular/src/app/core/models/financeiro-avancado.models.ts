export interface DefResumo { id: number; descricao?: string; }
export interface EtoResumo { id: number; obra?: string; }
export interface FluxoCaixaConsolidadoItem { id: number; descricao?: string; valor?: number; }
export interface PedidoInternoResumo { id: number; codigo?: string; status?: string; }
