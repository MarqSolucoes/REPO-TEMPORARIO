export interface FaturamentoResumo { id: number; cliente?: string; valor?: number; }
export interface FolhaPagamentoResumo { id: number; colaborador?: string; valor?: number; }
export interface CustoFixoResumo { id: number; descricao?: string; valor?: number; }
